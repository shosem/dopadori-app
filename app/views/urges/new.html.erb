<div class="flex flex-col items-center gap-10 py-8" data-controller="urge-button">

  <%# フェーズが変わるたびに書き換わるので、読み上げを追従させる %>
  <p class="min-h-9 text-2xl font-semibold text-primary"
     data-urge-button-target="label"
     aria-live="polite">
    押すと始まります
  </p>

  <%# 36秒の完了時に JS から requestSubmit() する。中身は要らない %>
  <%= form_with url: urges_path, data: { urge_button_target: "form" } do %><% end %>

  <%# 外周のリングは動かさない(拡縮アニメーションは Day 6)。
      円は scale(1.4) = 268px まで膨らむので、内側のリング(272px)にちょうど収まる。
      吸い切ると輪に届く、という関係になっている。 %>
  <div class="relative flex h-80 w-80 items-center justify-center">
    <div class="absolute inset-0 rounded-full border border-primary/15"></div>
    <div class="absolute inset-6 rounded-full border border-primary/25"></div>

    <button
      type="button"
      aria-label="衝動ボタン"
      class="relative h-48 w-48 rounded-full bg-primary-soft shadow-soft select-none touch-manipulation"
      data-urge-button-target="circle"
      data-action="click->urge-button#start"
    ></button>
  </div>

  <%# 初見の人が何をすればいいか分かるように置く。数値は説明であって記録ではない %>
  <div class="w-full rounded-card border border-outline/30 bg-card/70 p-6 text-center backdrop-blur-sm">
    <p class="text-ink">連打しながら3-3-6呼吸を行ってください</p>

    <div class="mt-4 flex items-center justify-center gap-2">
      <span class="flex h-8 w-8 items-center justify-center rounded-full bg-primary text-sm font-semibold text-white">3</span>
      <span class="text-ink-sub">-</span>
      <span class="flex h-8 w-8 items-center justify-center rounded-full bg-primary-soft text-sm font-semibold text-primary">3</span>
      <span class="text-ink-sub">-</span>
      <span class="flex h-8 w-8 items-center justify-center rounded-full bg-primary-soft text-sm font-semibold text-primary">6</span>
    </div>
  </div>
</div>
